# GPU dependencies for the Geneformer stage (fine-tuning + in silico perturbation).
# Requires an NVIDIA GPU. For an RTX 5090 (Blackwell), use PyTorch 2.6+ and CUDA 12.8+.

# Core ML
torch>=2.6.0
transformers>=4.46,<4.47
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.0
datasets>=2.20.0

# Geneformer (installed separately from source, see Dockerfile / scripts/00_fetch_geneformer.py)
#   pip install git+https://huggingface.co/ctheodoris/Geneformer

# Single-cell + omics
scanpy>=1.10.0
anndata>=0.10.0
cellxgene-census>=1.15.0

# Mechanism inference
decoupler>=1.8.0
gseapy>=1.1.0

# Data / infra
numpy>=1.26.0,<2.0
pandas>=2.2.0
pyarrow>=16.0.0
scipy>=1.13.0
scikit-learn>=1.5.0

# Orchestration
hydra-core>=1.3.2
omegaconf>=2.3.0
mlflow>=2.15.0

# Plotting
matplotlib>=3.8.0
seaborn>=0.13.0
